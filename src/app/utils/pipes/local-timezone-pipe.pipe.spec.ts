import { LocalTimezonePipePipe } from './local-timezone-pipe.pipe';

describe('LocalTimezonePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalTimezonePipePipe();
    expect(pipe).toBeTruthy();
  });
});
