import { DrErrorInputsDirective } from './dr-error-inputs.directive';

describe('DrErrorInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new DrErrorInputsDirective();
    expect(directive).toBeTruthy();
  });
});
