import { BtnUploadFileDirective } from './btn-upload-file.directive';

describe('BtnUploadFileDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnUploadFileDirective();
    expect(directive).toBeTruthy();
  });
});
