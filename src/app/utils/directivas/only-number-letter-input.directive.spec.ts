import { OnlyNumberLetterInputDirective } from './only-number-letter-input.directive';

describe('OnlyNumberLetterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberLetterInputDirective();
    expect(directive).toBeTruthy();
  });
});
